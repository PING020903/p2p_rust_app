//! 应用层（L3+）：按应用细分，应用内再按运行模式分 `cli`/`gui` 子目录。
//!
//! 职责边界：
//! - 应用逻辑（登录菜单/表单编排、命令树、会话循环、各类确认交互）都在这里
//! - 触碰核心只有两条合法通道：调用 `p2p` 的领域 API；经 `LineSource`/sink 做 I/O
//! - 交互流程永不进 `p2p`——协议核心无渲染、无菜单、无表单编排

pub mod chat;
pub mod file_transfer;
